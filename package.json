{
  "name": "pouchdb-commit-log",
  "description": "cluster-friendly append-only topic-based commit log + materialized view implementation for pouchdb (inspired by apache kafka and samza)",
  "version": "0.2.2",
  "repository": {
    "url": "nrw/pouchdb-commit-log"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "tape test/*.js",
    "watch": "nodemon -q -x 'tape test/*.js' | tap-colorize"
  },
  "dependencies": {
    "bluebird": "^2.9.8",
    "eventemitter2": "^0.4.14",
    "lodash.throttle": "^3.0.0",
    "monotonic-timestamp": "0.0.9"
  },
  "devDependencies": {
    "memdown": "^1.0.0",
    "pouchdb": "^3.3.0",
    "tape": "^3.5.0"
  }
}
